import React from "react";

const AdBanner = () => (
  <div style={{
    width: '100%',
    minHeight: '80px',
    background: '#ddd',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    color: '#444',
    borderRadius: '8px',
    margin: '16px 0'
  }}>
    <span>إعلان تجريبي</span>
  </div>
);

export default AdBanner;
